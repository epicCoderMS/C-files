#include<iostream>
#include<cmath>

using namespace std;
int main()
{
    double x = 3.4;
    double y = 4;
    double z ;
     
     //z = max(x,y);
    // z = min(x,y);

    //z = pow(2,3);
    //z = sqrt(16);
    // z = abs(-16);

   // z = round(x);
    // z = ceil(x);
    // z = floor(x);


     cout<< z<<'\n';
    return 0;
}